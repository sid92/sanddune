.venv/
__pycache__/
*.pyc
.DS_Store

# secrets
config.yaml

# runtime state (per-day/per-trigger detector state, not code) - includes
# state/crops/, which holds real camera frames, not just JSON
state/

# model weights - large binaries, fetch via gguf/README instead of committing
gguf/**/*.gguf
gguf/run_*_raw.log

# compiled Go binaries - build with `go build` inside backend/, don't commit
/sanddune
/sanddune.exe
backend/sanddune
backend/sanddune.exe
/cameracheck
/cameracheck.exe
backend/cameracheck
backend/cameracheck.exe
camera_captures/
